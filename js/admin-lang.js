/* ════════════════════════════════════════
   admin-lang.js — OBSOLÈTE

   La traduction de l'espace admin (FR/AR) est désormais gérée
   entièrement côté serveur via includes/lang_helper.php (fonction
   t(), $_SESSION['admin_language']) — voir la sidebar pour le
   sélecteur de langue. Chaque page admin recharge de toute façon
   au clic, donc un rendu 100% PHP est plus simple et fiable qu'un
   swap JS côté client (pas de désynchronisation possible).

   Ce fichier est gardé vide (et non supprimé) car il reste inclus
   par plusieurs pages admin existantes ; le supprimer casserait ces
   balises <script src="../js/admin-lang.js"> sans raison.
════════════════════════════════════════ */
